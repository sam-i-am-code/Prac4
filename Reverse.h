#ifndef REVERSE_H
#define REVERSE_H

#include <string>

using namespace std;

class Reverse
{
private:

public:
    Reverse(/* args */);
    int reverseDigit(int value);
    string reverseString(string letters);
    ~Reverse();
};
#endif